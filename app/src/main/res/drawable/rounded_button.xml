<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#007E22" /> <!-- Button background color -->
    <corners android:radius="16dp" />   <!-- Radius for round corners -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp" />        <!-- Optional padding inside the button -->
    <stroke
        android:width="2dp"
        android:color="#000000" />    <!-- Optional border color and width -->
</shape>
